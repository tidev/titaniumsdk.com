## Install the Titanium CLI

The same on every platform, which is why it lives in one file.

:::only macos, linux

:::code-group

@tab npm

```sh
sudo npm install -g titanium
```

@tab Yarn

```sh
sudo yarn global add titanium
```

:::

:::

:::only windows

```powershell
npm install -g titanium
```

:::
